<!-- 매장 썸네일 배경(라운드 코너 + 클립용). clipToOutline과 함께 쓴다. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/bg_surface"/>
    <corners android:radius="12dp"/>
</shape>
